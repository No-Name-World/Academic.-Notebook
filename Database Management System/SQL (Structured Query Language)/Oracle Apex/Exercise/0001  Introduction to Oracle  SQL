# Oracle Application Express (APEX) Exercises

## Exercise 1: Student Table

### a) Create a table called `student` with the following attributes:

- `name` as `VARCHAR2` with the size of 20 characters
- `gender` as `CHAR` of 6 characters
- `DOB` as `DATE`
- `CGPA` as `NUMBER` with one decimal

#### Answer:
```sql
CREATE TABLE student (
    name VARCHAR2(20),
    gender CHAR(6),
    DOB DATE,
    CGPA NUMBER(2,1)
);
